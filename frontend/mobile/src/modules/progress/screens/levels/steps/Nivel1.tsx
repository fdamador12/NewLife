import React from 'react';
import NivelModulo from '../NivelModulo';

export function Nivel1Modulo1({ navigation }: any) {
    return <NivelModulo navigation={navigation} level={1} sublevel={1} />;
}
export function Nivel1Modulo2({ navigation }: any) {
    return <NivelModulo navigation={navigation} level={1} sublevel={2} />;
}
export function Nivel1Modulo3({ navigation }: any) {
    return <NivelModulo navigation={navigation} level={1} sublevel={3} />;
}