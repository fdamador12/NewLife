import React from 'react';
import NivelModulo from '../NivelModulo';

export function Nivel8Modulo1({ navigation }: any) {
    return <NivelModulo navigation={navigation} level={8} sublevel={1} />;
}
export function Nivel8Modulo2({ navigation }: any) {
    return <NivelModulo navigation={navigation} level={8} sublevel={2} />;
}
export function Nivel8Modulo3({ navigation }: any) {
    return <NivelModulo navigation={navigation} level={8} sublevel={3} />;
}